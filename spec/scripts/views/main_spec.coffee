describe 'app.views.Main', ->
  beforeEach ->
    # json = loadJSONFixtures('main').main
    # @testCtor = app.views.Main
    # @subject = new @testCtor
    #   el: @fixture
    #   model: new Backbone.Model json
    # @subject.render()

  # afterEach ->
  #   @subject.close()
  #
  # it 'constructor - exists', ->
  #   expect(@testCtor).toBeDefined()

  # it 'internal model defined', ->
  #   expect(@subject.model).toBeDefined()
  #   expect(@subject.model.attributes).toBeDefined()
  #
  # it 'json fixture matches json', ->
  #   expect(@subject.model.get 'crap').toEqual "test crap"
  #
  # it 'was rendered', ->
  #   element = $(".crap")
  #   expect(element).toBeDefined()
  #   expect(element.length > 0).toBeTruthy()
